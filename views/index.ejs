<%- include("templates/header") %>
<div class="px-4 py-5 my-5 text-center">
    <img style="width: 300px" src="/image/angry.jpg" alt=""/>
    <div class="col-lg-6 mx-auto">
        <% if (isLoggedIn) { %>
            <a class="btn btn-primary btn-lg px-4 gap-3" href="/members">Go to Members Area</a>
        <% } else { %>
            <h1 class="display-5 fw-bold text-body-emphasis">Welcome to our website!</h1>
        <% } %>
    </div>
</div>

<%- include("templates/footer") %>