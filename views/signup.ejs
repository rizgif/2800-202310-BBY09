<% if (isLoggedIn) { %>
    <%- include("templates/header") %>
<% } else { %>
    <%- include("templates/header-beforeLogin") %>
<% } %>
<main class="form-wrapper signin">
    <div class="form-signup w-100 m-auto">
        <form action='/signup-submit' method='post' class="needs-validation" novalidate>
            <h1 class="h3 fw-normal">Create user</h1>

            <div class="form-floating">
                <input name="email" type="email" class="form-control" id="email" placeholder="name@example.com" required>
                <label for="email">Email address <span>*</span></label>
                <div class="invalid-feedback">
                    Please enter an email address.
                </div>
            </div>

            <div class="form-indication">
                <p>Password must be between 4 and 20 characters long.</p>
            </div>
            <div class="form-floating">
                <input name="password" type="password" class="form-control" id="floatingPassword" placeholder="Password" required min="2" max="20">
                <label for="password">Pasword <span>*</span></label>
                <div class="invalid-feedback">
                    Please choose a password.
                </div>
            </div>

            <div class="form-indication">
                <p>Name must be between 3 and 20 characters long.</p>
            </div>
            <div class="form-floating">
                <input name="username" type="text" class="form-control" id="username" placeholder="Username" required min="3" max="20">
                <label for="username">Name <span>*</span></label>
                <div class="invalid-feedback">
                    Please enter a name.
                </div>
            </div>

            <button class="w-100 btn btn-lg btn-primary" type="submit">Sign up</button>
        </form>
    </div>
</main>
<%- include("templates/footer") %>