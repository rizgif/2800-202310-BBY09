<% if (whichCourse) { %>
  <% reviewSliderPairs.forEach(pair=> { %>
    <%- include("read-review", { review: pair.review, sliderValue: pair.sliderValue }) %>
      <% }) %>

        <% } else {%>
          <%- include("allreviews", { review: pair.review, sliderValue: pair.sliderValue }) %>
            <% let courseContentSum = 0; %>
              <% reviewSliderPairs.forEach(pair=> { %>


                <% courseContentSum += paresInt(pair.sliderValue.courseContentSliderValue); %>
                  <% }); %>

                    <% console.log(courseContentSum); %>


                      <% }%>